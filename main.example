# Rename this file to `main.rule` and adjust the patterns to match your needs.
# Each non-empty line is treated as a regular expression.  Patterns are applied
# globally to the file contents, so use capturing groups and boundaries to
# narrow matches when required.

# IPv4 addresses
(?:\d{1,3}\.){3}\d{1,3}

# IPv6 addresses (simplified form)
(?:[0-9a-fA-F]{0,4}:){2,7}[0-9a-fA-F]{0,4}
